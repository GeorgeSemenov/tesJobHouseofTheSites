include ../domain-names/domain-names.pug
include ../servers/servers.pug
mixin domains-and-servers-container(modifier)
  .domains-and-servers-container&attributes(attributes)
    .content-container
      +domain-names(modifier.domainNames)
      +servers(modifier.servers)