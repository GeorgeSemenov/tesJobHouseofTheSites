mixin hosting-prices__link(modifier)
  if modifier == undefined
    - modifier = {};
  if modifier["href"] == undefined
    - modifier["href"] = "#!";
  if modifier["text"] == undefined
    - modifier["text"] = "empty__text";
  a.hosting-prices__link(href=modifier.href)&attributes(attributes)!=modifier.text
