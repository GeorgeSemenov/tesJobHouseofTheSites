mixin hosting-prices__proposals-side(modifier)
  .hosting-prices__proposals-side&attributes(attributes)
    +hosting-prices__proposals-title(modifier)
    +advantages-list(modifier.advatageList)
    //- +hosting-prices__proposals(modifier)