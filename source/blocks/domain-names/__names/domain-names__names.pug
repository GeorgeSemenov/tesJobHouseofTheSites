mixin domain-names__names(modifier)
  if modifier["names"] == undefined
    - modifier["names"] = [{}];
  ul.domain-names__names&attributes(attributes)
    each name in modifier.names
      if name["modifier"] == undefined
        - name["modifier"] = "";
      +domain-names__name(name)(class=name.modifier)