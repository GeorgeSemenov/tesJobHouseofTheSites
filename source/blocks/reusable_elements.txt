button
blockHead = h2 + desc // встречается почти в каждом блоке. У desc должен быть модификатор, который
			//будет регулировать его длину.

logoGrid - сетка логотипов, должен быть модификатор _withDividers

contacts - встречаются в хедере и в футере
